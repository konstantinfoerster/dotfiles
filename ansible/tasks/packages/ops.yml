---
- name: Install vm and ops packages
  community.general.pacman:
    name:
      - virtualbox-host-dkms
      - ansible
      - ansible-lint
      - python-passlib
      # ansible testing
      - molecule
      - python-docker
      # k8s
      - age
      - age-plugin-yubikey
      - sops
      - kubectl
      - kubectx
      - kustomize
    state: present
